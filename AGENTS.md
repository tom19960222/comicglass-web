# Project Rules

- HTML generated by the listing route is an invariant. Keep it byte-for-byte identical unless the user explicitly requests an HTML format change.
- Any route or rendering change must keep or update exact HTML output tests in `test/server.test.js`.
